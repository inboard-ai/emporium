[package]
name = "kv-repl"
version = "0.0.1"
edition = "2024"
publish = false

[dependencies]
emporium = { path = ".." }
futures = "0.3"
serde_json = "1.0"
tokio = { version = "1", features = ["full", "io-std"] }
