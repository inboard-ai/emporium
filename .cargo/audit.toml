[advisories]
# No ignores: the wasmtime 44 bump cleared the whole 43.0-stack cluster
# (rustls-webpki 0.102 cert/CRL advisories + the wasmtime-wasi path_open
# TRUNCATE write-perm bypass). Keep this empty — add an entry only with a
# tracked upstream rationale.
ignore = []
